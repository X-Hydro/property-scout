# Activated via SPRING_PROFILES_ACTIVE=local (same convention as
# auction-scout-manager) -- set that as a VM option or environment
# variable in the IntelliJ run configuration for local debugging.
#
# Matches the DSN already confirmed working from the Python side:
# postgresql://oncoord:oncoord-pg1@localhost:5432/property-scout
spring.datasource.url=jdbc:postgresql://localhost:5432/property-scout
spring.datasource.username=oncoord
spring.datasource.password=oncoord-pg1
spring.datasource.driver-class-name=org.postgresql.Driver